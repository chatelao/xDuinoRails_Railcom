name: Continuous Integration

on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Install RP2040 Platform
        run: |
          arduino-cli core update-index
          arduino-cli core install rp2040:rp2040@3.1.1 --additional-urls https://github.com/earlephilhower/arduino-pico/releases/download/global/package_rp2040_index.json

      - name: Compile All Sketches
        uses: arduino/compile-sketches@v1
        with:
          fqbn: rp2040:rp2040:pico
          libraries: |
            - name: AUnit
            - name: NmraDcc
          sketch-paths: |
            - examples/AccessoryDecoderDummy
            - examples/AccessoryDecoderNmra
            - examples/CommandStation
            - examples/FunctionDecoder
            - examples/LocomotiveDecoderDummy
            - examples/LocomotiveDecoderNmra
            - tests/DCCMessageTest
            - tests/EncodingTest
            - tests/InCircuitTest
            - tests/ManagerTest
            - tests/EndToEndTest
